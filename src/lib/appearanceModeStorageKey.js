/**
 * MUI default `mui-mode` is shared across many sites and often ends up set to
 * `light` from older builds/tests. Use a site-specific key so new sessions
 * default to System unless this key is set.
 */
export const APPEARANCE_MODE_STORAGE_KEY = 'cfriedric-appearance-mode';
