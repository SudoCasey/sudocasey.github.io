# Copy to .env.local and fill in. See ask-api/README.md for running the Ask API in Docker.

# Required for "Ask about Casey" when using the Next.js API route (same origin)
GEMINI_API_KEY=

# Optional: use a separate Ask API (e.g. Docker). Must end with /api/ask
# Example when running ask-api container and forwarding port 3001:
# NEXT_PUBLIC_ASK_API_URL=http://172.17.48.1:3001/api/ask
# NEXT_PUBLIC_ASK_API_URL=
